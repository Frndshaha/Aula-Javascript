_463‍.x([["somar",()=>somar],["calcularMediaEAprovacao",()=>calcularMediaEAprovacao]]);function somar(a, b) {
    return a + b;
}

       function calcularMediaEAprovacao(notas, mediaMinima)
{
    const soma = notas.reduce((acc, nota) => acc + nota, 0);
    const media = soma / notas.length;

    if (media >= mediaMinima) 
    {
        return "Aprovado";
    } else 
    {
        return "Reprovado";
    }
}
